name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: build docker
      run: |
        echo start
        docker build -t gha:latest -f docker/Dockerfile .

